# Google Gemini API 키 (4단계, 무료 티어). https://aistudio.google.com/apikey 에서 무료 발급.
# 비어 있으면 자연어 편집 지시는 규칙 기반(정규식/키워드) 파서로 대체 동작한다.
GEMINI_API_KEY=

# 업로드/생성 결과 파일 저장 위치
STORAGE_DIR=./data

# CORS 허용 origin (Flutter 데스크톱 앱 등), 콤마로 구분
ALLOWED_ORIGINS=*
